{
  "buildCommand": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/workout-tracker run build",
  "outputDirectory": "artifacts/workout-tracker/dist/public",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
